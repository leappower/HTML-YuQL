{
  "name": "static-cache-server",
  "version": "1.0.0",
  "description": "Simple static server with caching headers and gzip compression",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.18.2"
  }
}
